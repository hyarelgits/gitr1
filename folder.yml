- name: Create a directory using Ansible
  hosts: webserver
  tasks:
    - name: Ensure the directory /example/directory exists
      ansible.builtin.file:
        path: /example/test123
        state: directory
        mode: '0755' # Optional: Set permissions
